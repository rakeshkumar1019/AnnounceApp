# kind: Service
# apiVersion: v1
# metadata:
#   name: nodeapp
# spec:
#   selector:
#     app: nodeapp
#   ports:
#   - protocol: TCP
#     port: 80
#     targetPort: 8080
#   type: LoadBalancer   

kind: Service
apiVersion: v1
metadata:
  name: tomcat-service
spec:
  type: LoadBalancer
  selector:
    app: tomcat
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
